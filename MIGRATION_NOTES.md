# ChatGPT Sites → Astro 재구현 메모

## 구현 방향

- 목업 1의 얇은 상단 내비게이션 구조
- 목업 3의 중앙형 모듈 레이아웃
- 제공된 가로 배너를 실제 Hero 이미지로 사용
- 제공된 정사각형 이미지를 About 이미지로 사용
- 어두운 배경, 흰 텍스트, 옅은 시안 포인트
- CSS Grid / Flexbox 기반
- 간단한 hover 및 viewport reveal만 사용
- 서버, DB, 로그인, 전용 플랫폼 API 없음

## Sites와 달라질 수 있는 부분

ChatGPT Sites URL은 자동화된 외부 fetch 환경에서 계속 401을 반환하여 생성된 내부 소스와 정확한 픽셀값을 직접 가져오지는 못했습니다.
따라서 이 프로젝트는 다음 자료를 기준으로 수동 재구현했습니다.

- 대화에서 확정한 페이지 구조
- Sites 제작용 최종 프롬프트
- 생성된 목업 이미지
- 사용자가 제공한 배너 및 프로필 이미지
- 업로드된 포트폴리오 PDF의 작품 정보와 이미지

## 후속 정밀 비교 방법

Sites 화면과 Astro 화면을 같은 폭에서 캡처한 뒤 아래 항목을 조정하면 됩니다.

- `--max` 최대 콘텐츠 폭
- `--gutter` 좌우 여백
- `--section` 섹션 간격
- Hero 배너 높이
- 카드 열 수와 gap
- 제목 font-size / line-height
- 헤더 높이

주요 값은 `src/styles/global.css` 상단에 모여 있습니다.
